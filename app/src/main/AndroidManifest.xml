<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tuempresa.mensajeria">

    <!-- Permisos de Internet si la app los necesita -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MensajeriaApp">

        <!-- SplashActivity es la launcher -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />

        <!-- MenuActivity -->
        <activity
            android:name=".ui.MenuActivity"
            android:exported="false" />

        <!-- ChatActivity -->
        <activity
            android:name=".ui.ChatActivity"
            android:exported="false" />

        <!-- UserManagementActivity -->
        <activity
            android:name=".ui.UserManagementActivity"
            android:exported="false" />

    </application>

</manifest>
